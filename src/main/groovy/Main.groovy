class Main {

    static void main(String[] args) {

//        String equation = "9 + 5 * (5 / 5) + (1 + (2 + 4))"
        String equation = "2*(-2*3+(1+1))"
        Calculator calculator = new Calculator( )
        println(calculator.solveEquation(equation))
        String message = "asd";
        def testString = "${message}"

        2-(-1)
    }

}